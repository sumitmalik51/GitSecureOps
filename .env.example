# Environment Configuration for GitSecureOps

# Copy this file to .env.local and fill in your values

# GitHub OAuth App Configuration
# Create a GitHub OAuth App at: https://github.com/settings/applications/new
VITE_GITHUB_CLIENT_ID=your-github-client-id

# Function App URL (for OAuth callback) - Updated Architecture
VITE_FUNCTION_APP_URL=https://func-gh-dev.azurewebsites.net

# Static Web App URL (for local development)
VITE_STATIC_WEB_APP_URL=http://localhost:5173

# OAuth Redirect URI (should point to Function App - Updated Architecture)
VITE_GITHUB_REDIRECT_URI=https://func-gh-dev.azurewebsites.net/api/github-callback

# Azure OpenAI Configuration for Smart Recommendations (Optional)
# Configure these if using AI features - DO NOT commit real values
VITE_AZURE_OPENAI_ENDPOINT=https://your-openai-instance.openai.azure.com/
VITE_AZURE_OPENAI_API_KEY=your_azure_openai_api_key_here
VITE_AZURE_OPENAI_DEPLOYMENT_NAME=gpt-4o
VITE_AZURE_OPENAI_API_VERSION=2024-04-01-preview

# Instructions:
# 1. Copy this file to .env.local: cp .env.example .env.local
# 2. Update VITE_GITHUB_CLIENT_ID with your GitHub OAuth App Client ID
# 3. Update URLs to match your environment (dev/staging/prod)
# 4. Azure OpenAI is already configured with your credentials
# 5. Start the development server: npm run dev
# 6. OAuth flow: Frontend → GitHub → Function App → Frontend (with session token)

# Note: Don't commit .env.local to version control!

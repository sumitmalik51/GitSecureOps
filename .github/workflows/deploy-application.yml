name: Deploy Application to Azure Static Web Apps

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to deploy to'
        required: true
        default: 'dev'
        type: choice
        options:
        - dev
        - staging
        - prod

env:
  NODE_VERSION: '20.x'

jobs:
  deploy-application:
    runs-on: ubuntu-latest
    
    steps:
    - name: 'Checkout GitHub Action'
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: ${{ env.NODE_VERSION }}
        cache: 'npm'

    - name: Login to Azure
      uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}

    - name: Deploy Frontend to Static Web Apps
      run: |
        RESOURCE_GROUP="rg-gitsecureops-${{ inputs.environment }}"
        SWA_NAME=$(az staticwebapp list --resource-group $RESOURCE_GROUP --query "[0].name" -o tsv)
        SWA_URL=$(az staticwebapp list --resource-group $RESOURCE_GROUP --query "[0].defaultHostname" -o tsv)
        FUNCTION_APP_NAME="func-gh-${{ inputs.environment }}"
        
        # Install frontend dependencies and build
        npm ci
        # Use Function App URL for OAuth callback (not Static Web App)
        VITE_GITHUB_CLIENT_ID=${{ secrets.GH_WEB_APP }} VITE_GITHUB_REDIRECT_URI=https://$FUNCTION_APP_NAME.azurewebsites.net/api/github-callback npm run build
        
        # Install SWA CLI
        npm install -g @azure/static-web-apps-cli
        
        # Deploy frontend only to Static Web Apps (API stays on Function App)
        SWA_TOKEN=$(az staticwebapp secrets list --resource-group $RESOURCE_GROUP --name $SWA_NAME --query "properties.apiKey" -o tsv)
        echo "Deploying frontend to Static Web Apps production environment..."
        echo "Frontend: ./dist"
        swa deploy ./dist --deployment-token "$SWA_TOKEN" --env production --verbose
        
        echo "✅ Frontend deployed: https://$SWA_URL"
        echo "🔗 OAuth Callback URL: https://$FUNCTION_APP_NAME.azurewebsites.net/api/github-callback"

    - name: Deploy Function App
      run: |
        RESOURCE_GROUP="rg-gitsecureops-${{ inputs.environment }}"
        FUNCTION_APP_NAME="func-gh-${{ inputs.environment }}"
        
        # Install Function App dependencies
        cd api
        if [ -f "package-lock.json" ]; then
          npm ci
        else
          npm install
        fi
        
        # Install Azure Functions Core Tools
        npm install -g azure-functions-core-tools@4 --unsafe-perm true
        
        # Deploy Function App
        echo "Deploying Function App: $FUNCTION_APP_NAME"
        func azure functionapp publish $FUNCTION_APP_NAME --javascript --build-native-deps
        
        echo "✅ Function App deployed: https://$FUNCTION_APP_NAME.azurewebsites.net"
        cd ..

    - name: Update Function App Settings for OAuth
      run: |
        RESOURCE_GROUP="rg-gitsecureops-${{ inputs.environment }}"
        FUNCTION_APP_NAME="func-gh-${{ inputs.environment }}"
        SWA_URL=$(az staticwebapp list --resource-group $RESOURCE_GROUP --query "[0].defaultHostname" -o tsv)
        
        if [ -n "$SWA_URL" ]; then
          echo "Updating Function App FRONTEND_URL setting to: https://$SWA_URL"
          az functionapp config appsettings set \
            --resource-group "$RESOURCE_GROUP" \
            --name "$FUNCTION_APP_NAME" \
            --settings "FRONTEND_URL=https://$SWA_URL" \
            --output table
          echo "✅ Function App FRONTEND_URL updated for OAuth redirect fix"
        else
          echo "⚠️ Warning: Could not retrieve Static Web App URL"
        fi

    - name: Azure logout
      run: |
        az logout
      if: always()